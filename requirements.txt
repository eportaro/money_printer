# ─── Polymarket BTC Prediction Bot — Dependencies ────────────────────────────
# Install with: pip install -r requirements.txt

# Core data & ML
numpy>=1.26
pandas>=2.0
# Pinned: model_artifacts/*.pkl are pickled under 1.8; sklearn 1.9 moved private
# modules (_loss) and cannot unpickle them. Bump only together with a retrain.
scikit-learn==1.8.0

# HTTP
requests>=2.31
pyodbc>=5.1

# Websockets (tick_collector.py: Coinbase + Polymarket CLOB streams)
websockets>=12.0

# Web server
flask>=3.0
flask-cors>=4.0

# Environment config
python-dotenv>=1.0

# ── Optional: for Chainlink oracle direct queries ──
# web3>=6.0   # Uncomment if you want direct on-chain baseline reads

# ── Optional: for Jupyter exploration ──
# jupyterlab>=4.0
# matplotlib>=3.8
# seaborn>=0.13
